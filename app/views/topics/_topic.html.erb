<div class="topic">
  <%= profile_img(topic.user) %>
  <p><%= "#{topic.user.name}" %></p>
  <%= link_to topic_path(topic) do %>
    <p><%= topic.content %></p>
    <%= show_picture(topic) %>
  <% end %>
</div>
